# Leave unset for local dev (Vite proxies /api to http://localhost:4000).
# In production, point this at your deployed API's origin, no trailing slash,
# e.g. https://driver-space-api.onrender.com
VITE_API_BASE_URL=
