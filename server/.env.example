PORT=4000
JWT_SECRET=change-me-to-something-random
# Your deployed client's origin(s), comma-separated, e.g.:
# CORS_ORIGIN=https://your-site.netlify.app
CORS_ORIGIN=
JITTER_RADIUS_METERS=400
STALE_AFTER_MS=720000
SEED_ADMIN_PASSWORD=admin123
SEED_DRIVER_PASSWORD=driver123
# Optional: absolute path to a persistent disk mount for the SQLite file
# (see render.yaml). Defaults to ./data alongside this app.
DATA_DIR=
